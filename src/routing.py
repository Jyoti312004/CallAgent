from django.urls import path

# Define your websocket patterns here
websocket_urlpatterns = [
    # Example: re_path(r'ws/voice/(?P<room_name>\w+)/$', consumers.VoiceConsumer.as_asgi()),
    # For now, just an empty list if you don't have consumer classes yet
]
